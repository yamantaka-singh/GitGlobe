/**
 * Superseded by verify-world.ts, which checks the graph as well as the tiles.
 *
 * This file is safe to delete.
 */
console.error('verify-tiles.ts has been replaced by verify-world.ts — run `npm run verify` instead.');
process.exit(1);
