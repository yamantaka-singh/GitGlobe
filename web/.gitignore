node_modules/
dist/
.DS_Store
*.local

# Real dataset lives in git — `npm run gen:world` only makes synthetic
# placeholder data, not the pipeline-sourced tiles this repo ships.

# Planet diagnostics — reproducible with `npm run preview:planet`
public/planet-preview.png
public/planet-globe.png
.vercel
